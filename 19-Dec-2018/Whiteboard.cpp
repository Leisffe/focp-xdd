int k, x, n;
char buf[100]
fgets(buf, 100, stdin)

buf[99]=0;  //buffer of data

sscanf //reads data not from the keyboard but from the buffer of the memory
k=sscanf(buf, "%d", &x);

k=sscanf(buf, "%d%n", &x, &n)

"34, 56"
buf=34 
buf+n=56



#define WORD replacement xyz
#define MAX(a,b)

(a)>(b)? (a):(b)
x=MAX(3,5)

#define SQR(x)	(x)*(x)
SQR(z+1)
(z+1)*(z+1)

x=MAX3(1,3,2)





//you can use it e.g. during debugging etc
#if 0 
#endif
	return 0;



#define DEBUGMODE 1

...

#if DEBUGMODE

